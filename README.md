this application was created with replit agent, you can see the old version github.com/atknony/TusQuzGame

in this version Node.js backend deleted, Django backend implemented. Now localy working with django and react vite. 

all replit files deleted. 
